package com.hs.contract.model.constant;

/**
 * Nguồn gốc mẫu hợp đồng:
 * SYSTEM  — mẫu do admin quản lý, dùng chung cho toàn hệ thống
 * LANDLORD — mẫu do chủ nhà tự tải lên và sở hữu
 */
public enum ContractTemplateSource {
    SYSTEM,
    LANDLORD
}
